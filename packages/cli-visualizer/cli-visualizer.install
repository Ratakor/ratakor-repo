post_install() {
  cat <<EOF
  Example configuration files can be found in /usr/share/doc/cli-visualizer/.
  Configuration files are located in ~/.config/vis or whatever is your
  XDG_CONFIG_HOME. cli-visualizer doesn't conflict with vis.
EOF
}

post_upgrade() {
	post_install
}
